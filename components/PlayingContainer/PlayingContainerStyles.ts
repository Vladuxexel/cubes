import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rowsContainer: {
        flexDirection: 'row',
        width: '80%',
        height: '80%',
        borderWidth: 1,
        borderColor: '#fff',
        margin: '10%'
    },
    text: {
        color: '#fff'
    }
});